streamlit==1.26.0  # Ensure compatibility with the multipage setup
Pillow==9.1.1  # For image handling and camera capture
pytesseract==0.3.9  # For optical character recognition (OCR) to extract text from prescriptions
opencv-python==4.6.0  # For camera handling and image processing
pandas==2.0.3  # For data handling (meds.csv)
numpy==1.25.2  # For any numerical operations or data processing
gTTS==2.3.0  # Google Text-to-Speech to convert extracted text into audio
scikit-learn==1.2.2  # For building the medicine name matching ML model
joblib==1.3.1  # For saving/loading the trained ML model
